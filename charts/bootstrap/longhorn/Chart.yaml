apiVersion: v2

name: longhorn-bootstrap
description: A Helm Chart for deploying longhorn with minimal config during bootstrap
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: longhorn
    version: "1.3.0"
    repository: https://charts.longhorn.io
