apiVersion: v2

name: cilium-bootstrap
description: A Helm Chart for deploying cilium with minimal config during bootstrap
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: cilium
    version: "1.11.4"
    repository: https://helm.cilium.io/
